# Plan d'implémentation : le Bureau d'Eschaton

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** La VM de dogfooding boote en session graphique Hyprland + DankMaterialShell thémée Eschaton, avec deux plugins système fonctionnels — mise à jour et rollback du système depuis le shell — le tout livré en paquets `arch=(any)`.

**Architecture:** Zéro compilation : toute la pile vient des dépôts officiels (Arch + ALARM, versions identiques, veille 2026-08-28). Eschaton livre : un meta-paquet `eschaton-desktop`, un paquet de configuration (`eschaton-desktop-config` — Lua Hyprland, session, greetd par drop-in systemd), et deux plugins DMS système sous `/etc/xdg/quickshell/dms-plugins/` (pacman-owned, lecture seule pour DMS). Les inconnues d'exécution (rendu virtio-gpu, mécanisme de thème DMS) se résolvent par spikes en VM avant d'être packagées.

**Tech Stack:** hyprland (config **Lua** exclusivement), dms-shell-hyprland, quickshell, dgop, greetd, btrfs-assistant, QML/plugin.json (API DMS), polkit rules (JS), snapperd D-Bus, bats/shellcheck/qmllint/jq, VM UTM aarch64 (console série).

**Spec:** `docs/superpowers/specs/2026-08-28-bureau-design.md` (+ veille : `docs/veille/2026-08-28-sp2-bureau.md`, autorité factuelle datée)

## Global Constraints

- Exécution dans le worktree `socle` existant (branche unique jusqu'à la fusion du Socle — voir note de séquencement en fin de plan).
- Tous les paquets SP2 sont `arch=(any)` ; **aucun vendoring, aucun `-git`, aucune recompilation** de la pile bureau (interdits de veille §7.2).
- Config Hyprland en **Lua uniquement** ; l'entrée de session lance **`Hyprland -c ~/.config/hypr/hyprland.lua`** (invariant : sans `-c`, le Lua est ignoré silencieusement).
- Propriété de `~/.config/hypr/` : **DMS possède `dms/`** (réécrit par `dms setup`), Eschaton ne dépose RIEN dedans ; les défauts Eschaton vivent dans `/usr/share/eschaton/hypr/` ; l'utilisateur possède `hyprland.lua` (amorcé par `/etc/skel`).
- `requires_dms` **obligatoire** dans chaque `plugin.json` ; les plugins se jugent contre `extra/dms-shell` (1.5.3), jamais contre `master`.
- Plugins système : `/etc/xdg/quickshell/dms-plugins/<id>/` — jamais le chemin utilisateur, jamais `plugins.lock.json`.
- Motif LICENSE du Socle (symlink `LICENSE -> ../../LICENSE` + install), `sha256sums=(SKIP…)` locaux, pkgrel bump à tout changement de paquet déjà publié.
- Landlock : `--disable-sandbox` sur tout pacman en conteneur ; builds meta en `tools/build-pkg <pkg> -d`.
- VM : pilotage console série (tools/vm-dev.md §5), mot de passe `seylar`/`eschaton`, jamais `ping`, correctifs toujours dans le repo (+ CI verte avant re-test si paquet).
- Les tests dans la VM se font sur les paquets **publiés** (CI → Pages → `pacman -Syu`), pas sur des fichiers copiés à la main — c'est le test du flux de mise à jour réel.

---

### Task 1 : Spike — rendu Quickshell/DMS sous virtio-gpu (VM)

Risque n°10 de la veille : le seul inconnu technique sérieux. Sortie : une décision documentée, pas de code gardé.

**Files:**
- Modify: `tools/vm-dev.md` (nouvelle section « Spike rendu bureau »)

**Interfaces:**
- Produces: la décision « rendu OK / rendu via repli logiciel / bloqué » consommée par toutes les tâches VM (7–9) ; la liste des paquets réellement installés (consommée par Task 3 pour figer les depends).

- [ ] **Step 1 : Installer la pile bureau dans la VM (installation MANUELLE de spike — les paquets Eschaton n'existent pas encore)**

Par la console série (VM démarrée, session `seylar`) :

```bash
sudo pacman -Syu --noconfirm
sudo pacman -S --noconfirm hyprland dms-shell-hyprland pipewire wireplumber xdg-desktop-portal-hyprland ttf-jetbrains-mono-nerd noto-fonts
```

Noter les versions exactes installées (`pacman -Q hyprland dms-shell quickshell dgop`).

- [ ] **Step 2 : Lancer une session Hyprland depuis la console**

```bash
dms setup   # génère ~/.config/hypr/hyprland.lua + dms/*.lua — observer ce qu'il crée exactement (ls -la ~/.config/hypr/)
Hyprland -c ~/.config/hypr/hyprland.lua &
```

(Depuis un TTY : Hyprland démarre en Wayland direct. S'il refuse hors seat graphique, passer par `seatd`/logind — documenter le chemin qui marche.)

- [ ] **Step 3 : Vérifier le rendu et la vie du shell**

```bash
sleep 15
dms ipc call mpris getall 2>&1 | head -5     # le shell répond-il ?
hyprctl clients; hyprctl monitors            # le compositeur tourne-t-il ?
pgrep -af 'quickshell|dms'                    # les process vivent-ils ?
```

Critères : Hyprland stable ≥ 5 min, process DMS vivants, IPC répond. Rendu visuel : capturer la fenêtre UTM depuis le Mac (AppleScript/`screencapture` de la fenêtre — méthode à établir et documenter ; en dernier recours, constat indirect par hyprctl + absence de crash dans `journalctl --user`).

- [ ] **Step 4 : Si le rendu échoue (crash GPU, écran noir)**

Tester le repli logiciel : `QSG_RHI_BACKEND=software dms run` (et/ou `WLR_RENDERER`/variables Hyprland équivalentes — consulter les logs d'erreur d'abord). Documenter précisément l'erreur et le repli qui fonctionne.

- [ ] **Step 5 : Documenter la décision + l'inventaire dans `tools/vm-dev.md` et committer**

Y consigner aussi : ce que `dms setup` a créé (arbre exact), la liste `pacman -Q` des versions, la méthode de capture d'écran retenue. Nettoyer la VM du spike n'est PAS nécessaire (la Task 7 réinstallera par paquets par-dessus).

```bash
git add tools/vm-dev.md
git commit -m "docs: spike rendu Quickshell/DMS sous virtio-gpu (décision + inventaire dms setup)"
```

---

### Task 2 : Paquet `eschaton-desktop-config`

**Files:**
- Create: `packages/eschaton-desktop-config/PKGBUILD` (+ symlink `LICENSE`)
- Create: `packages/eschaton-desktop-config/eschaton-defaults.lua` (→ `/usr/share/eschaton/hypr/eschaton-defaults.lua`)
- Create: `packages/eschaton-desktop-config/skel-hyprland.lua` (→ `/etc/skel/.config/hypr/hyprland.lua`)
- Create: `packages/eschaton-desktop-config/eschaton.desktop` (→ `/usr/share/wayland-sessions/eschaton.desktop`)
- Create: `packages/eschaton-desktop-config/greetd-eschaton.toml` (→ `/etc/eschaton/greetd.toml`, `backup=`)
- Create: `packages/eschaton-desktop-config/greetd-override.conf` (→ `/usr/lib/systemd/system/greetd.service.d/eschaton.conf`)
- Create: `packages/eschaton-desktop-config/51-eschaton-desktop.preset` (→ `/usr/lib/systemd/system-preset/`)
- Test: assertions `tar -tf` + `luac -p` en conteneur

**Interfaces:**
- Consumes: constats du spike (Task 1) sur l'arbre `dms setup`.
- Produces: session `eschaton` (entrée Wayland), greetd auto-configuré par drop-in, amorce `hyprland.lua` du skel. Dépendance de `eschaton-desktop` (Task 3).

- [ ] **Step 1 : Écrire `skel-hyprland.lua`** (l'amorce utilisateur — 3 requires, la frontière de propriété §4 de la spec)

```lua
-- Point d'entrée Hyprland d'Eschaton (à toi : ce fichier t'appartient, il n'est jamais réécrit).
-- Ordre : défauts Eschaton -> arbre géré par DMS -> tes surcharges (dms/binds-user.lua).
dofile("/usr/share/eschaton/hypr/eschaton-defaults.lua")
local dms = os.getenv("HOME") .. "/.config/hypr/dms"
if io.open(dms .. "/colors.lua") then
  for _, f in ipairs({ "colors", "outputs", "layout", "cursor", "binds", "windowrules", "binds-user" }) do
    local path = dms .. "/" .. f .. ".lua"
    if io.open(path) then dofile(path) end
  end
end
```

(Adapter la liste des fichiers à ce que le spike a réellement observé de `dms setup` — c'est l'interface Task 1 → Task 2. Si `dms setup` génère lui-même un `hyprland.lua` : notre skel le REMPLACE pour l'utilisateur créé par l'installeur ; documenter dans le PKGBUILD que `dms setup` ne doit être invoqué qu'après premier login, et vérifier en Task 7 qu'il respecte le nôtre — l'upstream déclare ne pas toucher aux fichiers hors `dms/`.)

- [ ] **Step 2 : Écrire `eschaton-defaults.lua`** (défauts minimaux v1 : clavier fr, autostart DMS, fond d'écran)

```lua
-- Défauts Eschaton (pacman-owned). Surcharge : ~/.config/hypr/hyprland.lua ou dms/binds-user.lua.
input { kb_layout = "fr" }
exec_once { "dms run" }
misc { disable_hyprland_logo = true }
```

(Syntaxe exacte de l'API Lua Hyprland 0.56 à confronter à la doc `hypr.land/news/26_lua` et aux fichiers générés par `dms setup` vus au spike — corriger si l'API réelle diffère, c'est le point à vérifier de cette tâche.)

- [ ] **Step 3 : Écrire l'entrée de session et le greetd**

`eschaton.desktop` :

```ini
[Desktop Entry]
Name=Eschaton
Comment=Bureau Eschaton (Hyprland + DMS)
Exec=Hyprland -c %h/.config/hypr/hyprland.lua
Type=Application
```

(`%h` n'est pas expansé par greetd : utiliser la forme shell `bash -lc 'Hyprland -c "$HOME/.config/hypr/hyprland.lua"'` si le test Task 7 montre que la variable ne passe pas — l'invariant `-c` est non négociable, la forme exacte se valide en VM.)

`greetd-eschaton.toml` (auto-login de dogfooding, dette SP4 assumée — spec §7 risque 6) :

```toml
[terminal]
vt = 1

[initial_session]
command = "bash -lc 'exec Hyprland -c \"$HOME/.config/hypr/hyprland.lua\"'"
user = "seylar"

[default_session]
command = "agreety --cmd 'bash -lc \"exec Hyprland -c $HOME/.config/hypr/hyprland.lua\"'"
user = "greeter"
```

`greetd-override.conf` (le drop-in qui évite tout conflit de possession avec le `config.toml` de greetd) :

```ini
[Service]
ExecStart=
ExecStart=/usr/bin/greetd --config /etc/eschaton/greetd.toml
```

`51-eschaton-desktop.preset` :

```text
enable greetd.service
```

- [ ] **Step 4 : PKGBUILD** (motif Socle : `arch=(any)`, LICENSE symlink, `backup=(etc/eschaton/greetd.toml)`, installs `-Dm644` aux chemins ci-dessus)

- [ ] **Step 5 : Vérifier** — `luac -p` sur les deux .lua (conteneur : `pacman -S --noconfirm --disable-sandbox lua`), `tools/build-pkg eschaton-desktop-config -d`, `tar -tf` liste les 7 fichiers aux chemins exacts. Commit `feat: paquet eschaton-desktop-config (Lua, session, greetd par drop-in)`.

---

### Task 3 : Meta-paquet `eschaton-desktop` + fond d'écran

**Files:**
- Create: `packages/eschaton-desktop/PKGBUILD` (+ symlink LICENSE)
- Modify: `packages/eschaton-branding/` (fond d'écran + pkgrel bump)

**Interfaces:**
- Consumes: inventaire du spike (Task 1) — la liste de depends est celle qui a réellement fait tourner la session.
- Produces: `pacman -S eschaton-desktop` = tout le bureau. Consommé par Task 7.

- [ ] **Step 1 : Fond d'écran** — générer une image sombre simple committée dans `eschaton-branding` :

```bash
tools/sandbox -c 'pacman -S --noconfirm --disable-sandbox imagemagick >/dev/null && magick -size 2560x1440 gradient:"#0d0b1a-#2a1e4d" packages/eschaton-branding/wallpaper-default.png'
```

Installer → `/usr/share/backgrounds/eschaton/default.png` ; `pkgrel` bump de branding. (Le branchement du wallpaper dans DMS se règle en Task 7 sur constat du mécanisme réel — le fichier, lui, est livré dès maintenant.)

- [ ] **Step 2 : PKGBUILD `eschaton-desktop`** — meta `arch=(any)`, `depends=(hyprland dms-shell-hyprland pipewire wireplumber xdg-desktop-portal-hyprland greetd polkit btrfs-assistant ttf-jetbrains-mono-nerd noto-fonts eschaton-desktop-config eschaton-dms-plugin-update eschaton-dms-plugin-rollback)` — ajusté à l'inventaire du spike. Commentaire : compositeur substituable (`dms-shell-niri` fournit le même virtuel) ; btrfs-assistant = dette avec condition de sortie.

- [ ] **Step 3 : Build `-d`, tar/`.PKGINFO` vérifiés, commit** `feat: meta-paquet eschaton-desktop + fond d'écran branding`. (Les deux plugins n'existant pas encore, le paquet se committe mais ne se publie qu'après Tasks 5–6 — ordre de merge des commits sans importance, la CI ne publie que ce qui buildte : garder ce build local jusqu'à Task 6 incluse, OU committer avec les depends plugins et accepter un échec CI transitoire — choisir la première option : cette tâche committe SANS pousser, le push arrive en Task 6.)

---

### Task 4 : Vérification bi-architecture des dépendances (CI)

**Files:**
- Create: `tools/check-desktop-deps`
- Modify: `.github/workflows/ci.yml` (job lint : + shellcheck du script + exécution)

**Interfaces:**
- Produces: garde CI — toute dépendance d'`eschaton-desktop` absente d'un des deux dépôts (API Arch / index miroir ALARM) fait échouer le lint. (Recommandation de veille §7.3, pendant SP2 du smoke x86_64.)

- [ ] **Step 1 : Écrire le script** — extrait `depends=()` du PKGBUILD d'eschaton-desktop (en excluant les `eschaton-*`, servis par notre dépôt), interroge `https://archlinux.org/packages/search/json/?name=<pkg>` (résultat non vide) et l'index `http://mirror.archlinuxarm.org/aarch64/extra/` + `/core/` (présence de `<pkg>-*.pkg.tar.xz`), échoue en listant les manquants. `set -euo pipefail`, curl `-fsS`, shellcheck silencieux.
- [ ] **Step 2 : Brancher au job lint de ci.yml** (ubuntu, curl/jq disponibles), tester en local, commit `feat: garde CI bi-architecture des dépendances du bureau`.

---

### Task 5 : Plugin `eschaton-dms-plugin-update`

**Files:**
- Create: `packages/eschaton-dms-plugin-update/PKGBUILD` (+ LICENSE symlink)
- Create: `packages/eschaton-dms-plugin-update/plugin.json`
- Create: `packages/eschaton-dms-plugin-update/EschatonUpdateWidget.qml`
- Create: `packages/eschaton-dms-plugin-update/EschatonUpdateSettings.qml` (minimal)
- Test: validation `jq` du manifest + `qmllint` en conteneur

**Interfaces:**
- Consumes: l'API plugins DMS — **étude dirigée obligatoire au Step 1** (les imports `qs.*` ne sont pas une API stable, on copie les motifs des plugins first-party).
- Produces: `/etc/xdg/quickshell/dms-plugins/eschatonUpdate/` ; dépendance d'`eschaton-desktop`. Testé en réel en Task 8.

- [ ] **Step 1 : Étude dirigée (1 h max)** — cloner `https://github.com/AvengeMedia/dms-plugins` en zone jetable, lire UN plugin `widget` simple de bout en bout (manifest + QML + héritage `PluginComponent` + usage `qs.Common`/`qs.Widgets`) et la doc versionnée 1.5 (`DankLinux-Docs`, `versioned_docs/version-1.5/…/plugin-development`). Consigner dans le rapport : le squelette minimal réel d'un widget (imports exacts, propriétés obligatoires), le mécanisme d'exécution de commandes sous permission `process`, l'id de bar widget. NE PAS vendorer — c'est une étude.
- [ ] **Step 2 : `plugin.json`** (anatomie établie par la veille §3.1, `requires_dms` obligatoire) :

```json
{
  "id": "eschatonUpdate",
  "name": "Eschaton — Mises à jour",
  "version": "0.1.0",
  "type": "widget",
  "component": "./EschatonUpdateWidget.qml",
  "settings": "./EschatonUpdateSettings.qml",
  "permissions": ["process", "settings_read", "settings_write"],
  "requires_dms": ">=1.5.0"
}
```

- [ ] **Step 3 : Le widget QML** — structure calquée sur le motif first-party du Step 1 : un indicateur de barre (icône + badge du nombre de mises à jour, rafraîchi périodiquement par `checkupdates` — paquet `pacman-contrib`, lecture seule, à ajouter aux depends du plugin) et une action au clic qui exécute `eschaton-update --yes` dans un terminal de sortie visible (ou la surface que le motif first-party utilise pour montrer un flux — reprendre son mécanisme, pas l'inventer) puis notifie si un reboot kernel est requis (le message d'eschaton-update l'écrit déjà).
- [ ] **Step 4 : PKGBUILD** (`arch=(any)`, `depends=(dms-shell pacman-contrib)`, install du répertoire complet vers `/etc/xdg/quickshell/dms-plugins/eschatonUpdate/`) ; validation : `jq -e '.id and .name and .version and .type and .component and .requires_dms' plugin.json`, `qmllint` (conteneur : `pacman -S --disable-sandbox qt6-declarative`) en tolérant les warnings d'imports `qs.*` non résolus hors DMS (documenter la commande exacte et son verdict), build `-d`, tar vérifié.
- [ ] **Step 5 : Commit** `feat: plugin DMS eschaton-update (badge + déclenchement de mise à jour)`.

---

### Task 6 : Plugin `eschaton-dms-plugin-rollback` + publication de la vague SP2

**Files:**
- Create: `packages/eschaton-dms-plugin-rollback/` (PKGBUILD + LICENSE + plugin.json + `EschatonRollbackWidget.qml` + settings)
- Create: `packages/eschaton-dms-plugin-rollback/50-eschaton-snapper.rules` (→ `/usr/share/polkit-1/rules.d/`)

**Interfaces:**
- Consumes: motifs de l'étude Task 5 ; D-Bus `org.opensuse.Snapper` (snapperd, déjà installé par le Socle).
- Produces: le différenciateur — snapshots listés et rollback déclenché depuis le shell. Publication : à la fin de cette tâche, push de TOUTE la vague SP2 (Tasks 2→6) et CI verte.

- [ ] **Step 1 : La règle polkit** :

```javascript
// Autorise le groupe wheel à piloter snapperd sans mot de passe depuis la session.
// Périmètre : uniquement les actions Snapper — le rollback système est PRÉCISÉMENT
// ce que ce shell doit offrir en un geste (spec Bureau §1).
polkit.addRule(function(action, subject) {
    if (action.id.indexOf("org.opensuse.Snapper.") === 0 && subject.isInGroup("wheel")) {
        return polkit.Result.YES;
    }
});
```

- [ ] **Step 2 : `plugin.json`** (id `eschatonRollback`, type `widget`, permissions `["process","settings_read"]`, `requires_dms >=1.5.0`) — le QML passe par `busctl`/`gdbus` sous permission `process` pour parler à snapperd (`busctl call org.opensuse.Snapper /org/opensuse/Snapper org.opensuse.Snapper ListSnapshots s root`) : moins élégant qu'un binding D-Bus natif mais dans le modèle documenté ; si l'étude Task 5 a révélé un accès D-Bus direct dans les motifs first-party, l'utiliser à la place et le documenter.
- [ ] **Step 3 : Le widget** — liste des snapshots (n°, date, description — parsing de la sortie busctl), bouton « Restaurer » avec confirmation à deux étapes, action = `CreateSnapshotV2`/rollback via l'API snapperd (méthode exacte relevée sur `busctl introspect` en VM — consigner) puis invite au reboot. Le premier livrable peut être read-only + rollback, PAS de comparaison de fichiers (v2).
- [ ] **Step 4 : PKGBUILD** (installe plugin + règle polkit), validations Task 5 (jq/qmllint), build `-d`.
- [ ] **Step 5 : Publication de la vague** — push des commits Tasks 2→6, `gh run watch --exit-status`, vérifier depuis la sandbox que `pacman -Si eschaton-desktop eschaton-dms-plugin-update eschaton-dms-plugin-rollback` répond sur le dépôt publié. Commit final `feat: plugin DMS eschaton-rollback (snapshots + restauration depuis le shell)`.

---

### Task 7 : Installation réelle du bureau en VM

**Files:**
- Modify: `tools/vm-dev.md` (section « Bureau »)

**Interfaces:**
- Consumes: dépôt publié (Task 6), VM post-Socle (Task 10 du Socle l'a laissée saine), spike (Task 1).
- Produces: la session graphique de référence, consommée par Tasks 8–9. Constats des mécanismes DMS (wallpaper, settings système) pour d'éventuels 7bis.

- [ ] **Step 1 : Installer par le flux réel** — `sudo pacman -Syu` (ramène la vague SP2) puis `sudo pacman -S --noconfirm eschaton-desktop` ; `sudo systemctl preset-all` (applique le preset greetd) — vérifier qu'AUCUNE autre étape manuelle n'est nécessaire, c'est le critère.
- [ ] **Step 2 : Reboot → constat de session** — greetd auto-login, Hyprland + DMS à l'écran. Preuves série : `pgrep -af 'Hyprland|quickshell|dms'`, `hyprctl monitors`, `loginctl` (session graphique seat0), `systemctl status greetd` ; preuve visuelle par la méthode de capture du spike. Vérifier l'invariant Lua : `hyprctl` confirme la config chargée (`hyprctl getoption` d'une valeur posée par eschaton-defaults.lua, ex. kb_layout fr).
- [ ] **Step 3 : Constats d'intégration à consigner** (chacun peut engendrer un correctif packagé en 7bis) : les 2 plugins Eschaton apparaissent-ils (DankBar/Control Center) ? btrfs-assistant se lance-t-il (polkit OK) ? quel est le mécanisme réel de wallpaper/thème DMS (et brancher `/usr/share/backgrounds/eschaton/default.png` par le canal découvert) ? `dms setup` (s'il s'exécute) respecte-t-il notre `hyprland.lua` ?
- [ ] **Step 4 : vm-dev.md + commit** `feat: bureau Eschaton installé et démarré en VM (session Hyprland+DMS)` (+ commits 7bis séparés pour chaque correctif découvert, règle repo habituelle).

---

### Task 8 : Test réel des deux plugins (le différenciateur en conditions réelles)

**Files:**
- Modify: `tools/vm-dev.md`

**Interfaces:**
- Consumes: session de la Task 7.

- [ ] **Step 1 : Update depuis le shell** — provoquer une mise à jour disponible (au besoin en publiant un pkgrel bump anodin d'un paquet eschaton), constater le badge du plugin, déclencher la mise à jour DEPUIS le widget, vérifier : transaction pacman réelle exécutée, paire snap-pac créée (`snapper list`), entrées Limine resynchronisées, notification de reboot si kernel.
- [ ] **Step 2 : Rollback depuis le shell** — créer un marqueur (`touch ~/MARQUEUR-AVANT`), snapshot manuel via le widget si offert (sinon la paire de l'update du Step 1 sert de cible), modifier l'état (`sudo pacman -S --noconfirm cowsay` par exemple), puis DEPUIS le widget rollback : liste visible, restauration confirmée, reboot → `cowsay` absent, marqueur intact (le rollback ne touche pas /home). Sorties série à l'appui.
- [ ] **Step 3 : Chargement à chaud vérifié** — `dms ipc call plugins reload eschatonUpdate` fonctionne (mécanisme veille §3.1). Consigner + committer `test: plugins update et rollback validés en conditions réelles`.

---

### Task 9 : Clôture SP2

**Files:**
- Modify: `docs/superpowers/specs/2026-08-28-bureau-design.md` (statut), `tools/vm-dev.md` (checklist finale)

- [ ] **Step 1 : Dérouler la définition de terminé de la spec §6** (les 6 points, preuves à l'appui — le point 6 « zéro terminal » se vérifie en refaisant réseau/audio/affichage/update/rollback uniquement à la souris dans la session).
- [ ] **Step 2 : Statut spec → implémenté, tag `v0.2.0`, push.** Commit `docs: Bureau v0.2.0 — définition de terminé validée`.

---

## Note de séquencement

Ce plan s'exécute APRÈS la clôture du Socle (Tasks 11–12 du plan Socle + revue finale de branche). Si la fusion du Socle vers `main` intervient avant le SP2, l'exécution continue sur une branche `bureau` issue de `main` fraîche — les contraintes globales restent identiques ; la deployment branch policy Pages suit le même motif que le Socle.

## Couverture spec → tasks

| Spec Bureau | Tasks |
|---|---|
| §1 positionnement (rollback natif) | 6, 8 |
| §2 périmètre (session + 2 plugins) | 2, 3, 5, 6, 7, 8 |
| §3 architecture paquets (+ btrfs-assistant dette) | 2, 3, 5, 6 |
| §4 invariants Lua + propriété hypr/ | 2, 7 (step 2/3) |
| §5 flux de session | 2, 7 |
| §6 définition de terminé | 1 (spike), 7, 8, 9 + 4 (CI bi-arch) |
| §7 risques 1/2/4/5 | 1, tasks 5–6 (requires_dms), 4, 8 |
