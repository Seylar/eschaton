# Bilan d'exécution — Socle v0.1.0

- **Période** : 2026-08-27 → 2026-08-28. **Fusion** : [PR #1](https://github.com/Seylar/eschaton/pull/1), merge `80d6827`, tag `v0.1.0` = `1395680`.
- **Méthode** : subagent-driven development — 13 tâches (12 du plan + 7bis née de la spec révisée), un implémenteur frais ou repris par tâche (Opus), une revue par tâche + 6 fix rounds + une revue finale de branche (Fable), chaque fix re-revu en scope.
- **Preuves** : `tools/vm-dev.md` §11 (dossier de preuves §7.1-§7.4). Les rapports de tâches étaient des artefacts de session, non versionnés — ce bilan et le git en sont la trace durable.

## Rulings du contrôleur (exhaustif, ordre chronologique)

Chaque décision prise en autonomie, avec son coût si elle était fausse. Détail complet dans le message de session ; ici la liste de référence.

1. namcap informatif (`|| true`) — validation réelle = install VM. 2. `sha256sums=SKIP` locaux — intégrité par git. 3. `.gitignore` d'infra créé au setup, fusion (pas écrasement) en T2. 4. Interactions écran VM : scriptable d'abord, computer-use si accordé, jamais de sorties inventées. 5. `ping` → `curl` (ICMP bloqué par NAT UTM). 6. `tools/sandbox` : flags TTY conditionnels, puis token canonique `${a[@]+…}` (bash 3.2). 7. `--disable-sandbox` ratifié (Landlock pacman 7 sous Docker), étendu à `DisableSandbox` pacman.conf conteneur puis `-Syu --needed --disable-sandbox` (provision-gradle autosuffisant). 8. LICENSE MIT racine + motif symlink par paquet (le `../../` dans `source=()` étant impossible — vérifié dans makepkg). 9. Meta-paquets construits en `-d` (makepkg vérifie les depends runtime) ; vendorés aussi (makedepends résolus via la base pacman, pas le PATH). 10. Gradle : distribution officielle épinglée SHA-256 (`tools/provision-gradle`) — Arch/ALARM inutilisables ; dette avec condition de sortie. 11. Préset complété : `limine-snapper-sync.service` + `snapper-cleanup.timer` (rétention ET resync Limine). 12. Défaut de plan préflight T7 : `detect_arch` normalise `arm64`→`aarch64`. 13. Vérification post-install obligatoire de `/boot` (wrapper mkinitcpio ne propage pas les codes retour). 14. Repo GitHub public créé sur consentement explicite ; CI/deploy autorisés sur `socle` pendant le dev (policy supprimée à la fusion). 15. Spec-sync utilisateur intégrée : ESP 2→4 Gio, invariant `TARGET_OS_NAME`, dépôt par architecture (vendorés natifs), risque ALARM requalifié. 16. `TARGET_OS_NAME` livré via `/etc/default/limine` (chemin upstream prioritaire, possédé par personne). 17. Mot de passe VM de dev `eschaton` (documenté à changer). 18. Spec §6 amendée : rollback par méthode **replace** (`snapper rollback` = disposition openSUSE, réfuté T10 puis corroboré dans le code amont). 19. Contrat `--restore-kernels` vérifié statiquement (sources au tag épinglé) ; preuve dynamique déférée avec protocole (`vm-dev.md` §9.10). 20. Flake Gradle ARM : aucun retry automatique (masquerait les régressions) — relance manuelle. 21. Vague de fix finale : documentaire uniquement, les Minor code différés avec destinations (SP2/SP4). 22. Fusion par PR mergée via GitHub (checkout principal jamais touché, SHAs et tag préservés) — sous la directive `/goal`.

## Triage des 44 différés (revue finale)

- **Corrigés avant fusion** : spec Socle risque 8 au passé ; spec Bureau raccordée (méthode replace) ; renvois `.superpowers/` requalifiés ; run de clôture inliné ; 3 toilettages.
- **Soldés en route** : outillage série committé (`tools/vm-serial`), timeout/pty paramétrés, sémantique `/etc/default/limine` prouvée en réel, URL archboot avec repli.
- **SP2 (prioritaires)** : durcir `tools/vm-serial` (lint CI, EOF pty, rc) avant d'en dépendre ; UX du rollback (`@.avant-rollback-*`, cleanup errexit, mention /home) au moment du plugin ; motd/`url=`/métadonnées au premier bump de paquets ; flag `--arch` du dry-run.
- **SP4 (dossier ouvert)** : signature du dépôt ; `partprobe`/`udevadm settle` (vrai matériel) ; validation d'arguments installeur (`--disk` dernier token, `[[ -b ]]`, interpolation user/hostname) ; `visudo -cf` en CI ; `sshd` par défaut à re-peser ; actions CI épinglées par SHA ; `backup=` de `10-wheel.sudoers` ou convention documentée.
- **Surveillance** : double Limine x86_64 (spec §8.9) ; santé ALARM (spec §8.2/§8.1) ; gradle (retrait du provisioning quand Arch répare) ; préséance replace-vs-fusion sans objet.
- **Reste dû** : preuve dynamique du rollback à travers un changement de kernel — protocole exécutable à la première mise à jour de kernel ALARM (`vm-dev.md` §9.10).
